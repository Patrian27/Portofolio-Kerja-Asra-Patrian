{
  "buildCommand": "cd ../.. && pnpm install && npx vite build --config artifacts/portfolio/vite.config.vercel.ts",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
